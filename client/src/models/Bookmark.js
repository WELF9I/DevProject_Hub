export class Bookmark {
    constructor(id, clerkId, projectId, createdAt) {
      this.id = id;
      this.clerkId = clerkId;
      this.projectId = projectId;
      this.createdAt = createdAt;
    }
  }
  
  