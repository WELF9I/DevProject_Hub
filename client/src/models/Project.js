export class Project {
    constructor(id, title, description, stars, forks, watchers, tags, link, createdAt) {
      this.id = id;
      this.title = title;
      this.description = description;
      this.stars = stars;
      this.forks = forks;
      this.watchers = watchers;
      this.tags = tags;
      this.link = link;
      this.createdAt = createdAt;
    }
  }
  
  